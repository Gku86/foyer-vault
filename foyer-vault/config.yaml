name: "Foyer Vault"
version: "1.3.5"
slug: foyer_vault
description: "Coffre chiffré Foyer : JSON du budget + fichiers (photos, PDF) chiffrés. Home Assistant ne peut pas les ouvrir."
url: "https://github.com/Gku86/foyer-vault"
arch:
  - amd64
  - aarch64
  - armv7
startup: services
boot: auto
init: false
ports:
  8099/tcp: 8099
ports_description:
  8099/tcp: "API Foyer (LAN)"
options:
  secret: "changez-moi"
schema:
  secret: password
